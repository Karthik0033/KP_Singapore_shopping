import * as LucideIcons from 'lucide-react';
console.log(Object.keys(LucideIcons).filter(k => k.toLowerCase().includes('github') || k.toLowerCase().includes('facebook') || k.toLowerCase().includes('twitter') || k.toLowerCase().includes('youtube')));
